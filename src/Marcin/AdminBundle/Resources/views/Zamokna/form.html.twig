{% extends 'MarcinAdminBundle::base.html.twig' %}
{% block stylesheets %}
{{parent()}}
{% endblock %}
{% block body %}
            <section class="content-header">
          <h1>
            Edycja
            <small>Edycja okna</small>
          </h1>
          <ol class="breadcrumb">
            <li><a href="#"><i class="fa fa-dashboard"></i> Pulpit</a></li>
            <li class="active">tutaj</li>
          </ol>
        </section>

        <!-- Main content -->
        <section class="content">
            <div class="row">
                <div class="col-md-12">
                    <div class="box box-default color-palette-box">
                        <div class="box-header with-border">
                            <h3 class="box-title"><i class="fa fa-tag"></i> Edycja okna</h3>
                        </div>
                        <div class="box-body">

                         {% include 'MarcinAdminBundle:Template:flashMsg.html.twig' %}
                        {{ form_start(form, {'attr': {'novalidate': 'true'}}) }}
                        <div class="form-group">
                            {{ form_row(form.name) }}
                        </div>
                        <div class="form-group">
                            {{ form_row(form.felc) }}
                        </div>{# , {'attr': {'disabled':'true'} } #}
                        <div class="form-group">
                            {{ form_row(form.oscieznica) }}
                        </div>
                        <div class="form-group">
                            {{ form_row(form.skrzydlo) }}
                        </div>
                        <div class="form-group">
                            {{ form_row(form.blaszka) }}
                        </div>
                        <div class="form-group">
                            {{ form_row(form.rodzaj) }}
                        </div>
                        <div class="form-group">
                            {{ form_row(form.stronawiercenia) }}
                        </div>
                        <div class="form-group">
                            {{ form_row(form.stalaszer) }}
                        </div>
                        <div class="form-group">
                            {{ form_row(form.stalawys) }}
                        </div>
                        <div class="form-group">
                            {{ form_rest(form) }}
                        {{ form_row(form.save) }} 
                        </div>
                        {{ form_end(form) }}
                        <a href="{{ path('marcin_admin_zamokna') }}" class="btn btn-primary btn-block">Wstecz</a> 

                        </div>
                    </div>
                </div>
            </div>
</section>
        {% endblock %}
{% block javascripts %}
    {{parent()}}
{% endblock %}